// Trigger full crawl run
// Seeds all registered regulators for a complete extraction cycle

export {};


async function run() {
  console.log('Seeding all regulators...');
}

run().catch(console.error);
